# ACME HTTP-01 challenge for all domains + HTTP->HTTPS redirect
server {
    listen 80;
    server_name app.if-x.ru api.if-x.ru livekit.if-x.ru;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
